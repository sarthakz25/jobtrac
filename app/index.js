import { View, Text } from 'react-native';

// we can assume view as div and text as p or h tag

const Home = () => {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
}

export default Home;